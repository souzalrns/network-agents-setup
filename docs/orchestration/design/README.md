# Orquestração — vertical design

```text
objective → ux_flow → ui_spec → design_critic → HITL
```

- Agents: `agents/design/`
- Skills: `skills/design/`
- Knowledge: `docs/knowledge/design/`
- Template: `templates/design-flow.plan.yaml`

Wiring: mesmo padrão que marketing (`action` → agent + skill via plan_runner external).
