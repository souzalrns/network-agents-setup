# Agents

Definições de agent (identidade + skill + action).  
**Marketing P0:** [marketing/](./marketing/).

Integração com runner: mode `external` inclui `agent_path` e `skill_path` no `pending_steps/<id>/request.json`.
