apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: api
  namespace: network-agents
spec:
  rules:
  - host: api.network-agents.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: api
            port:
              number: 80
